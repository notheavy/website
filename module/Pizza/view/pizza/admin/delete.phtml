<?php 
/**
 * Website Zend\Together
 *
 * @package    Pizza
 * @author     Ralf Eggert <r.eggert@travello.de>
 * * @link       http://www.zf-together.com
 */

/**
 * namespace definition and usage
 */
use Pizza\Entity\PizzaEntity;

/**
 * Pizza-Admin delete view script
 * 
 * @package    Pizza
 */

$this->headTitle('Pizza löschen');
$this->pageTitle('Die ausgewählte Pizza löschen');

// set pizza entity
$pizza = $this->pizza; /* @var $pizza PizzaEntity */

// set show url
$urlShow = $this->url('pizza/url', array('url' => $pizza->getUrl()));
?>
<table class="table table-data">
<tbody>
<tr>
<th>Url:</th>
<td><a href="<?php echo $urlShow; ?>"><?php echo $urlShow; ?></a></td>
</tr>
<tr>
<th>Bild:</th>
<td><?php echo $this->pizzaShowPicture($this->pizza); ?></td>
</tr>
<tr>
<th>Name:</th>
<td><?php echo $pizza->getName(); ?></td>
</tr>
<tr>
<th>Preis:</th>
<td><?php echo $this->currencyFormat($pizza->getPrice()); ?></td>
</tr>
<tr>
<th>Beschreibung:</th>
<td><?php echo $pizza->getDescription(); ?></td>
</tr>
</tbody>
</table>
<?php 
$actionUrl = $this->url('pizza-admin/action', array(), array(), true);
echo $this->showForm($this->form, $actionUrl, 'form-horizontal'); 
